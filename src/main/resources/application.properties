# Tomcat server port
# server.port=7070

# Context path
# server.servlet.context-path=/day14_boot2

# Session management
# server.servlet.session.persistent=false

# Database connection properties
spring.datasource.url=jdbc:mysql://attendance.cvu0m6gqm7ra.us-east-1.rds.amazonaws.com:3306/attendanceapp
spring.datasource.username=admin
spring.datasource.password=admin123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update  # Creates or updates tables
spring.jpa.show-sql=true  # Show SQL queries in the console
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
